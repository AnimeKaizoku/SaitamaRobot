from SaitamaRobot import (DEV_USERS, DRAGONS, SUPPORT_USERS, TIGER_USERS,
                          WHITELIST_USERS, telethn)

IMMUNE_USERS = DRAGONS + WHITELIST_USERS + SUPPORT_USERS + TIGER_USERS + DEV_USERS

IMMUNE_USERS = list(DRAGONS) + list(WHITELIST_USERS) + list(
    SUPPORT_USERS) + list(TIGER_USERS) + list(DEV_USERS)
