name: Format Python code
on: 
  push: 
    branches: [shiken]
  pull_request:
    branches: [shiken]
jobs:
  Autoyapf:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Auto yapf
        uses: turquoise-giggle/yapf-action@master
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@master
        with:
          commit-message: Automated code formatting.
          committer: turquoise-giggle<62775010+turquoise-giggle@users.noreply.github.com>
          title: Automated code formatting.
          body: This is an automated code formatting pull request.
          labels: Code Formatting
          reviewers: turquoise-giggle 
          branch: autofix
